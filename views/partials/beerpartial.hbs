<section class="beerPartial">
    <div>
    {{#if this.image_url}}
    <img src="{{this.image_url}}" height="200px">
    {{/if}}
    </div>
    <div>
    <h2>{{this.name}}</h2>
    <h3>{{this.tagline}}</h3>
    <p>{{this.description}}</p> 
    </div>  
</section>